let card = document.querySelectorAll(".card");

for (i = 0; i <= card.length; i++) {
    card[i].addEventListener("click",(event)=>{alert("Escolha o seu.")});
}
function comprar() {
    alert(Prosseguir);
}